export function AuditWordmark({ className = "" }: { className?: string }) {
  return (
    <span
      className={`font-display text-xl font-bold text-rose-700 ${className}`}
    >
      Audit
    </span>
  );
}
